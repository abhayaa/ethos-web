import React from 'react';
import CheckoutForm from '../components/Stripe';


const stripe = () => {
    return (
        <div>
            <CheckoutForm />
        </div>
    )
}

export default stripe
